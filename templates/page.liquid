<div id="page" class="container valign-center">
    <div class="page-inner">
        <h1 class="heading page-title">{{ page.title }}</h1>
        <div class="page-content">{{ page.content }}</div>
    </div>
</div>
